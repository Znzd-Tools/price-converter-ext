export const EXT_PREFIX = 'price-converter-ext';
export const BADGE_CLASS = `${EXT_PREFIX}-badge`;
export const PICKER_ROOT_ID = `${EXT_PREFIX}-picker-root`;
export const INIT_KEY = '__priceConverterExtInit';

export const STORAGE_KEYS = {
  config: 'conversionConfig',
  pickedTarget: 'pickedTarget',
} as const;
